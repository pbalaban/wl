%h1 Listing exercises
%table
  %thead
    %tr
      %th Name
      %th Values type
      %th{:colspan => "3"}
  %tbody
    - @exercises.each do |exercise|
      %tr
        %td= exercise.name
        %td= exercise.values_type
        %td= link_to 'Show', [@place,exercise]
        %td= link_to 'Edit', edit_place_exercise_path(@place,exercise)
        %td= link_to 'Destroy', [@place, exercise], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Exercise', new_place_exercise_path(@place)
